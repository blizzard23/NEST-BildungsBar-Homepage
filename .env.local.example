# Supabase – Werte findest du im Supabase-Dashboard unter
# Project Settings -> API. Kopiere diese Datei zu ".env.local" und trage ein.
# In Vercel dieselben Variablen unter Settings -> Environment Variables setzen.
#
# WICHTIG: Das Projekt nutzt den neuen "Publishable Key" (Format sb_publishable_…).
# Wenn die Stellen/Blog in Produktion leer bleiben, ist meist noch ein alter
# (deaktivierter) anon-JWT-Key in Vercel hinterlegt – dann hier bzw. in Vercel
# den aktuellen Publishable Key eintragen. Dieser Key darf öffentlich sein.

NEXT_PUBLIC_SUPABASE_URL=https://nagqpfxbmbxzfemickpd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_rF6buUxNiHL37LtMAFj3AQ_jcQvVI5m

# Service-Role-Key (nur Server!) – nötig für den Erinnerungs-Cron.
# Supabase -> Project Settings -> API -> "service_role" secret. NIEMALS öffentlich machen.
SUPABASE_SERVICE_ROLE_KEY=dein-service-role-secret

# Schutz für den Cron-Endpoint (frei wählbares Geheimnis). Vercel sendet damit
# automatisch den Header "Authorization: Bearer <CRON_SECRET>".
CRON_SECRET=ein-langes-zufaelliges-geheimnis

# WhatsApp-Erinnerung (optional) – WhatsApp Cloud API (Meta).
# WHATSAPP_PHONE_ID = Telefonnummern-ID, WHATSAPP_TOKEN = dauerhaftes Access-Token,
# WHATSAPP_TEMPLATE = Name eines GENEHMIGTEN Templates mit genau einer {{1}}-Variable.
# WHATSAPP_TOKEN=...
# WHATSAPP_PHONE_ID=...
# WHATSAPP_TEMPLATE=terminerinnerung
# WHATSAPP_LANG=de

# Optional: Empfänger-Adresse für die mailto-Fallback-Buttons (Standard: info@nest-bildungsbar.de)
# NEXT_PUBLIC_TERMIN_MAIL=info@nest-bildungsbar.de

# ---------------------------------------------------------------------
# NESTplay-Anbindung (Partner-Portal-Dashboard zeigt Live-Spiele)
# Öffentliche Werte des NESTplay-Supabase-Projekts (Spiele sind public lesbar).
# Ohne diese Variablen bleibt das NESTplay-Feature inaktiv (zeigt einen Hinweis).
# ---------------------------------------------------------------------
# NEXT_PUBLIC_NESTPLAY_SUPABASE_URL=https://DEIN-NESTPLAY-PROJEKT.supabase.co
# NEXT_PUBLIC_NESTPLAY_SUPABASE_ANON_KEY=nestplay-anon-public-key
# NEXT_PUBLIC_NESTPLAY_URL=https://nest-play.de

# ---------------------------------------------------------------------
# SMTP – Mailversand über lima-city (Termin-, Kontakt- & Sonderanfragen)
# Werte aus deinem lima-city-Postfach (Webmail -> Einstellungen / Hilfe).
# ---------------------------------------------------------------------
SMTP_HOST=smtp.lima-city.de
SMTP_PORT=465
SMTP_SECURE=true              # true = SSL (Port 465), false = STARTTLS (Port 587)
SMTP_USER=postfach@deine-domain.de
SMTP_PASS=dein-postfach-passwort
MAIL_TO=info@nest-bildungsbar.de   # Wohin die Anfragen gehen
MAIL_FROM=postfach@deine-domain.de # Absender (muss zum Postfach passen)

